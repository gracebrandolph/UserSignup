@model List<UserSignup.Models.User>

<h1>User Page</h1>

<p><a asp-controller="User" asp-action="Add">Add User</a></p>

@if (Model.Count != 0)
{
    @foreach (var user in Model)
    {
        <p>Hello @user.Username</p>
    }
}
